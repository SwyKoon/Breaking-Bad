import Logo from '../img/logo.png'

const Headers = () => {
    return ( 
        <header className='center'>
            <img src={Logo} alt='' />
        </header>
    );
}
 
export default Headers;